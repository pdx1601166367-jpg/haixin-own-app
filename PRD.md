# 工作生活专属 App PRD

- 版本：v1.0
- 日期：2026-08-10
- 状态：v1.0 已实现并通过验收

## 1. 产品目标和使用场景

做一个只给用户本人使用的工作生活专属 App，把日常计划、自媒体、校招求职、产品工作、健身、饮食、娱乐集中在一个本地应用里管理，避免散落在不同工具中。

使用场景：

- 只在用户自己的电脑浏览器中运行
- 数据保存在当前电脑，刷新、关闭浏览器、重启电脑后不丢失
- 不需要登录、联网、云数据库、部署上线和手机端

## 2. 整体页面框架和导航结构

采用单页应用形式，桌面浏览器为主，左侧固定导航栏 + 右侧内容区。

导航结构（从上到下）：

1. 首页总览
2. 今日计划
3. 自媒体
4. 校招进展
5. 产品工作
6. 健身计划
7. 饮食计划
8. 游戏娱乐
9. 数据与设置

页面框架规则：

- 侧边栏固定，当前模块高亮，点击切换模块
- 顶部显示当前模块标题和常用操作入口（如“新增”）
- 所有新增统一使用“+ 新增”，表单放在弹窗中
- 编辑和删除在条目行内完成
- 删除和清空操作必须有二次确认
- 空状态不显示空白，显示引导文字和新增按钮

## 3. 首页内容结构

首页是总览页，包含以下内容：

- 今日日期和欢迎区
- 今日计划：今日任务清单 + 完成进度条
- 快速备忘：顶部输入框，回车即记；下方列表支持勾选完成和删除
- 各模块摘要卡片：展示每个模块最新且重要的摘要信息，点击卡片跳转到对应模块

## 4. 每个模块的用途

- 今日计划：按天安排当天要做的事，支持优先级、时间和完成状态
- 自媒体：管理内容创作流程（灵感、选题、草稿、发布）和账号数据
- 校招进展：管理求职投递全过程，跟踪每个公司当前处于哪个环节
- 产品工作：管理个人工作项目、需求池、迭代和日常工作日志
- 健身计划：安排每周训练，记录每次训练和身体指标
- 饮食计划：记录每天三餐和加餐，估算热量，记录饮水
- 游戏娱乐：管理游戏库、游玩记录和心愿单
- 数据与设置：查看数据概览、备份恢复数据、调整外观和模块开关

## 5. 每个模块第一版最必要的数据和操作

### 今日计划

数据：日期、任务标题、时间段、优先级、来源标签、是否完成。

操作：按日期切换；新增、编辑、删除任务；勾选完成；查看当天完成率。

### 自媒体

数据：平台账号（平台、账号名、粉丝数、作品数）；内容条目（标题、平台、状态、发布时间、备注）；按天数据（日期、播放量、涨粉数）。

操作：账号增删改；内容在“灵感 / 选题 / 草稿 / 已发布”之间流转；记录按天数据；查看发布日历。

### 校招进展

数据：公司、岗位、投递时间、当前状态（准备中 / 已投递 / 笔试 / 面试 / Offer / 已放弃）、下一步行动、截止时间、流程时间线、备注。

操作：新增投递记录；修改状态；添加流程节点和备注；按状态分组查看；查看顶部统计（投递总数、面试中、Offer 数）。

### 产品工作

数据：项目（名称、描述）；需求（标题、优先级 P0/P1/P2、状态 待排期/进行中/已完成/砍掉）；迭代计划；本周待办；工作日志。

操作：新增和管理项目；在项目内新增、编辑需求；维护待办和日志；勾选完成待办。

### 健身计划

数据：训练计划（名称、每周安排）；动作（名称、组数、次数、重量）；训练打卡记录；身体指标（日期、体重、体脂）。

操作：创建训练计划；按计划打卡记录实际数据；记录体重体脂并查看简单趋势。

### 饮食计划

数据：每日三餐和加餐记录（食物、大致热量）；常用菜谱库；饮水杯数。

操作：按日期切换；新增、编辑、删除餐食记录；查看当天合计热量；从菜谱库快速选择；饮水打卡。

### 游戏娱乐

数据：游戏库（名称、状态 在玩/想玩/已通关/弃坑、评分、感想）；游玩记录（日期、时长、感想）；心愿单（名称、预计价格）。

操作：游戏增删改；记录游玩；维护心愿单；查看本月游玩时长和游戏总数。

### 数据与设置

数据：各模块条目数量统计、备份导出文件、外观设置、模块开关。

操作：导出 JSON 备份；导入备份恢复（覆盖确认）；清空全部数据（二次确认）；切换主题色；调整列表密度；开关模块显示。

## 6. 首页、今日计划和各模块之间的关系

- 首页是总览入口，今日计划是首页最核心的内容
- 各模块产生的重要事项可以标记来源，并出现在今日计划中
- 今日计划任务可以带来源标签（如“校招”“产品工作”），点击可回到对应模块
- 首页的模块摘要卡片实时读取各模块最新数据，点击卡片进入对应模块
- 快速备忘独立存储，但展示在首页顶部区域
- 模块之间不共享业务数据，今日计划只引用来源标签，不复制其他模块的完整数据

## 7. 本地数据、备份和恢复要求

- 数据保存在浏览器本地存储（localStorage），关闭浏览器和重启电脑后不丢失
- 不联网、不连数据库，所有读写都在本机完成
- 本地数据结构包含版本号字段，便于后续升级时迁移
- 数据与设置模块提供“导出备份”：一键下载 JSON 文件
- 提供“恢复导入”：选择备份文件，确认后覆盖当前数据
- 提供“清空数据”：二次确认后清空全部数据
- 模块开关只控制显示/隐藏，不删除数据

## 8. 第一版必须完成的功能

- 9 个模块全部可用，侧边栏可切换，当前模块高亮
- 每个模块具备本 PRD 第 5 节列出的基础数据和操作，不是同一种任务列表换名字
- 首页展示今日计划、快速备忘和各模块摘要卡片，并能跳转
- 今日计划支持按日期查看和勾选完成
- 所有数据操作立即保存到本地，无需手动保存
- 支持导出备份、导入恢复和清空数据
- 支持模块开关、主题色和列表密度设置

## 9. 第一版暂时不做的功能

- 登录、联网、云同步、云数据库
- 多设备同步、部署上线、手机端适配
- 拖拽排序
- 系统级消息通知和自动提醒
- 复杂图表和数据分析
- 全局搜索
- 多人协作和账号体系
- 需要安装或配置服务器的运行方式

## 10. 可以实际检查的验收标准

1. 打开应用即可使用，不需要安装、启动服务或连接网络
2. 侧边栏包含 9 个模块，点击可切换，当前模块有高亮标识
3. 首页显示今日日期、今日计划进度、快速备忘和各模块摘要卡片
4. 快速备忘输入后回车立即出现在列表，刷新后仍然存在
5. 今日计划可切换日期，可新增、编辑、删除任务，勾选后完成率立即更新
6. 自媒体可新增账号，内容条目可在灵感、选题、草稿、已发布之间流转，可记录按天数据
7. 校招可新增投递记录，状态可流转，流程时间线可添加节点，顶部统计数字与记录一致
8. 产品工作可新增项目，项目内需求池、迭代、本周待办、工作日志都可增删改
9. 健身可创建含动作的训练计划，可打卡记录实际组数、次数、重量，可记录体重体脂
10. 饮食可按日期记录三餐和加餐，当天热量合计正确，可从菜谱库选择，可饮水打卡
11. 游戏娱乐可维护游戏库、游玩记录和心愿单，游玩时长统计正确
12. 任意模块新增数据后刷新浏览器，数据仍然存在
13. 关闭浏览器后重新打开，数据仍然存在
14. 数据与设置可导出 JSON 备份文件，文件可正常打开并包含各模块数据
15. 清空数据后各模块为空，导入备份后数据恢复到清空前状态
16. 删除条目和清空数据都出现二次确认
17. 关闭某个模块后，导航栏和首页摘要中不再显示该模块；重新开启后数据仍然存在
18. 切换主题色和列表密度后立即生效
19. 空状态页面显示引导文字和新增按钮
20. 所有数据操作即时生效，没有额外的手动保存按钮
